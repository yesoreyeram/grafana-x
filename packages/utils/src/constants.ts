/**
 * Default Yarn version used by the `setup-yarn` command when no version is
 * provided. Keep this in sync with the `packageManager` field of the root
 * `package.json`, which is the canonical pin for this repository.
 */
export const DEFAULT_YARN_VERSION = '4.16.0';
