apiVersion: 1

datasources:
  - name: ClickUp
    type: yesoreyeram-clickup-datasource
    access: proxy
    jsonData:
      baseURL: https://api.clickup.com/api
      authMethod: apiKey
    secureJsonData:
      apiKey: ${CLICKUP_API_TOKEN}
    editable: true
