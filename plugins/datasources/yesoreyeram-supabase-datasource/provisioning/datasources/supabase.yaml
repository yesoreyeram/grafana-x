apiVersion: 1

datasources:
  - name: Supabase
    type: yesoreyeram-supabase-datasource
    access: proxy
    isDefault: true
    jsonData:
      apiUrl: $SUPABASE_API_URL
    secureJsonData:
      serviceKey: $SUPABASE_SERVICE_KEY
    editable: true
