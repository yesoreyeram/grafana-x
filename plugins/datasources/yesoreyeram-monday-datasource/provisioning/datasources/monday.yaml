apiVersion: 1

datasources:
  - name: monday.com
    type: yesoreyeram-monday-datasource
    access: proxy
    jsonData:
      baseURL: https://api.monday.com/v2
      authMethod: apiKey
    secureJsonData:
      apiToken: ${MONDAY_API_TOKEN}
    editable: true
