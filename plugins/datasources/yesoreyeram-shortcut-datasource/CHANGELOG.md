# Changelog

## 0.1.0 (unreleased)

- Initial release
- Stories and Count query types
- Shortcut REST API v3 integration via the search endpoint
- API token authentication via the `Shortcut-Token` header (stored as a Grafana secret)
- Pagination over search results
- Story flattening into scalar columns for tabular consumption
