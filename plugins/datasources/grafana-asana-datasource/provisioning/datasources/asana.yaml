apiVersion: 1

datasources:
  - name: Asana
    type: yesoreyeram-asana-datasource
    access: proxy
    jsonData:
      baseURL: https://app.asana.com/api/1.0
    secureJsonData:
      apiKey: ${ASANA_API_TOKEN}
    editable: true
