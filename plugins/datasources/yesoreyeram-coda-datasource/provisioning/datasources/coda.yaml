apiVersion: 1

datasources:
  - name: Coda
    type: yesoreyeram-coda-datasource
    access: proxy
    isDefault: true
    jsonData:
      docId: $CODA_DOC_ID
    secureJsonData:
      apiToken: $CODA_API_TOKEN
    editable: true
