apiVersion: 1

datasources:
  - name: Todoist
    type: yesoreyeram-todoist-datasource
    access: proxy
    jsonData:
      baseURL: https://api.todoist.com/api/v1
    secureJsonData:
      apiToken: ${TODOIST_API_TOKEN}
    editable: true
